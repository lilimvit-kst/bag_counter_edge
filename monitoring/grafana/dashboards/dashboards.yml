apiVersion: 1

providers:
  - name: 'Bag Counter Edge Dashboards'
    orgId: 1
    folder: 'Operations'
    folderUid: 'operations'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
